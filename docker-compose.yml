version: "3.9"

services:
  posterbot:
    image: posterbot:latest
    build: .
    container_name: posterbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - BOT_ENV=production
    ports:
      - "${WEBHOOK_PORT:-8080}:8080"
    networks:
      - botnet

networks:
  botnet:
    driver: bridge
